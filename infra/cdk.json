{
  "$schema": "https://github.com/aws/aws-cdk/raw/v2-schema/schema/latest/schema.json",
  "app": "npx ts-node --swc --project tsconfig.json bin/cdk.ts",
  "watch": {
    "include": ["**/*.ts"],
    "exclude": ["cdk.out", "dist", "node_modules"]
  }
}
